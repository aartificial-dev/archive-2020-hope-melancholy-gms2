/// @description Insert description here
// You can write your code in this editor

if (signal_get(signal)) {
	if (is_method(func)) {
		func();
	} else if (script_exists(func)) {
		script_execute(func);
	}
	if (!constant) {
		instance_destroy();
	}
}